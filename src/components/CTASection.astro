---
import Container from "./Container.astro";
import Button from "./Button.astro";
import { LINKS } from "@/consts";

interface Props {
  title?: string;
  text?: string;
  primaryLabel?: string;
  primaryHref?: string;
  primaryExternal?: boolean;
  secondaryLabel?: string;
  secondaryHref?: string;
}
const {
  title = "Prêt à préparer votre prochain voyage ?",
  text = "Récupérez la checklist gratuite, puis téléchargez le guide complet pour partir avec un planning clair.",
  primaryLabel = "Télécharger la checklist gratuite",
  primaryHref = LINKS.freeChecklist,
  primaryExternal = true,
  secondaryLabel = "Voir les guides",
  secondaryHref = "/guides",
} = Astro.props;
---

<section class="py-16 sm:py-20">
  <Container>
    <div
      class="relative overflow-hidden rounded-[1.75rem] bg-gradient-to-br from-brand-600 via-brand-600 to-accent-600 px-6 py-12 text-center shadow-lift sm:px-12 sm:py-16"
    >
      <div
        class="pointer-events-none absolute -right-16 -top-16 h-56 w-56 rounded-full bg-white/10 blur-2xl"
        aria-hidden="true"
      >
      </div>
      <div
        class="pointer-events-none absolute -bottom-16 -left-10 h-56 w-56 rounded-full bg-white/10 blur-2xl"
        aria-hidden="true"
      >
      </div>
      <h2 class="relative text-2xl font-bold text-white sm:text-3xl">
        {title}
      </h2>
      <p class="relative mx-auto mt-3 max-w-2xl text-brand-50">{text}</p>
      <div
        class="relative mt-8 flex flex-col items-center justify-center gap-3 sm:flex-row"
      >
        <a
          href={primaryHref}
          target={primaryExternal ? "_blank" : undefined}
          rel={primaryExternal ? "noopener noreferrer" : undefined}
          class="inline-flex items-center justify-center gap-2 rounded-full bg-white px-7 py-3.5 text-base font-semibold text-brand-700 shadow-soft transition-all duration-200 hover:-translate-y-0.5 hover:shadow-lift"
        >
          {primaryLabel}
        </a>
        {
          secondaryHref && (
            <a
              href={secondaryHref}
              class="inline-flex items-center justify-center gap-2 rounded-full px-7 py-3.5 text-base font-semibold text-white ring-1 ring-white/40 transition-all duration-200 hover:bg-white/10"
            >
              {secondaryLabel}
            </a>
          )
        }
      </div>
    </div>
  </Container>
</section>
