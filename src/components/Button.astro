---
interface Props {
  href?: string;
  variant?: "primary" | "secondary" | "ghost";
  size?: "md" | "lg";
  external?: boolean;
  class?: string;
  type?: "button" | "submit";
  ariaLabel?: string;
}
const {
  href,
  variant = "primary",
  size = "md",
  external = false,
  class: className = "",
  type = "button",
  ariaLabel,
} = Astro.props;

const base =
  "inline-flex items-center justify-center gap-2 rounded-full font-semibold transition-all duration-200 focus-visible:outline-none disabled:opacity-60";

const sizes: Record<string, string> = {
  md: "px-5 py-2.5 text-sm",
  lg: "px-7 py-3.5 text-base",
};

const variants: Record<string, string> = {
  primary:
    "bg-brand-600 text-white shadow-soft hover:bg-brand-700 hover:shadow-lift hover:-translate-y-0.5",
  secondary:
    "bg-white text-ink-900 ring-1 ring-slate-200 shadow-soft hover:ring-brand-200 hover:-translate-y-0.5",
  ghost: "text-brand-700 hover:bg-brand-50",
};

const classes = `${base} ${sizes[size]} ${variants[variant]} ${className}`;
const rel = external ? "noopener noreferrer" : undefined;
const target = external ? "_blank" : undefined;
---

{
  href ? (
    <a
      href={href}
      class={classes}
      rel={rel}
      target={target}
      aria-label={ariaLabel}
    >
      <slot />
    </a>
  ) : (
    <button type={type} class={classes} aria-label={ariaLabel}>
      <slot />
    </button>
  )
}
